# Simple k-NN Decision Boundary Visualizer

Follow below to run the project locally:

```bash
# Clone the repository
git clone
cd knn-visualizer
# Install dependencies
npm install
# Start the development server
npm start
```
